let input =  window.prompt("Enter string")
let number = window.prompt("Enter number")

num = Number(num);

for(let i = 0; i < num; i++){
  window.alert(input)
}

// window.alert(input.repeat(number))
